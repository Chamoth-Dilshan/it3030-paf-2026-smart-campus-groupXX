function NotificationDrawer() {
  return (
    <div
      style={{
        minWidth: "260px",
        padding: "16px",
        borderRadius: "12px",
        background: "#ffffff",
        border: "1px solid #d9e2ec",
      }}
    >
      <strong>Notifications</strong>
      <p style={{ marginBottom: 0 }}>Placeholder drawer for alerts, updates, and reminders.</p>
    </div>
  );
}

export default NotificationDrawer;
